{
  "name": "zar-jobs-ai-connector",
  "version": "0.1.0",
  "description": "Read-only job discovery connector for Codex and MCP-compatible AI assistants.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check src/server.mjs && node --check src/portals/capabilities.mjs && node --check src/portals/url-normalizer.mjs && node --check scripts/validate-plugin.mjs",
    "test": "node --test",
    "validate:plugin": "node scripts/validate-plugin.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  }
}
